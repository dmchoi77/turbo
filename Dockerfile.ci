FROM node:18-bullseye

# pnpm 런타임만 세팅 (설치는 docker run 시점에 수행)
ENV PNPM_HOME=/usr/local/share/pnpm
ENV PATH="$PNPM_HOME:$PATH"

WORKDIR /workspace

RUN corepack enable \
  && corepack prepare pnpm@9.6.0 --activate

CMD ["bash"]


